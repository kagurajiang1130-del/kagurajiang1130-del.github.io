# 网站内容、语言与发布指南

## 最简单的修改方式

直接在 Codex 中说明要修改什么，并继续使用这个项目文件夹。例如：

- “把日语首页标题改成……”
- “同时修改三种语言的 Basic 套餐价格”
- “增加一个餐厅案例，并补齐日语、英语、中文”
- “替换首页和 About 的图片，然后重新发布”

Codex 可以直接在现有网站上修改并重新发布，不需要重建网站。

## 三语言内容在哪里

所有经常修改的文字集中在：

`app/content.ts`

文件内有三个语言区：

- `siteContent.ja`：日语，也是默认语言
- `siteContent.en`：英语
- `siteContent.zh`：简体中文

三种语言使用完全相同的数据结构和页面结构。修改服务、案例、价格或 FAQ 时，建议同时修改三个语言区，避免语言切换后内容不一致。

语言按钮名称与语言代码在同一文件的 `localeOptions` 中管理。

## 常用内容位置

| 内容 | 配置位置 |
| --- | --- |
| SEO 标题与说明 | `seo` |
| 品牌名称与简介 | `brand` |
| 导航 | `navigation` |
| 首页标题、按钮、切换文字 | `hero` |
| 各板块标题 | `sections` |
| 品牌理念 | `philosophy` |
| 客户问题 | `problems` |
| 解决思路 | `approach` |
| 服务 | `services` |
| 体验指南 | `experienceGuide` |
| 流程 | `process` |
| 案例 | `cases` |
| 套餐和价格 | `pricing` |
| 愿景 | `vision` |
| 团队与创始人 | `about` |
| 常见问题 | `faqs` |
| 邮箱、SNS、表单文字 | `contact` |
| 页脚与纹样文字 | `footer` |

手动修改时，只替换引号里的内容，尽量不要删除逗号、括号或字段名称。

## 添加服务、案例或 FAQ

复制同一数组里的完整项目，再替换内容。三种语言中的项目数量和顺序应保持一致。

例如，新增第四个案例时，需要在 `ja.cases`、`en.cases`、`zh.cases` 中分别增加对应内容。

## 图片

把图片放在：

`public/images/`

建议使用 WebP 或压缩后的 JPEG。当前页面保留设计化视觉占位区；提供正式图片后，可以让 Codex 把图片地址接入统一配置。

建议尺寸：

- 首屏图片：至少 1600px 宽
- 案例封面：4:3，至少 1200px 宽
- 团队照片：4:5，至少 1200px 宽

## 默认语言

默认语言为日语。访问者切换语言后，浏览器会记住选择，并在网址中显示 `?lang=ja`、`?lang=en` 或 `?lang=zh`。

## 本地运行

电脑需要安装 Node.js。进入项目文件夹后运行：

```text
npm install
npm run dev
```

根据终端显示的本地网址打开网站。发布前可以运行：

```text
npm run build
```

## 重新部署

保存修改后，在 Codex 中说“检查并重新发布网站”。Codex 会更新现有公开网站，网址保持不变。

当前联系表单只有完整界面，尚未连接真实邮件服务。正式收件前，需要接入表单或邮件服务，并把占位邮箱与 SNS 链接替换为真实内容。

## 设计与页面结构

- 页面结构和交互：`app/page.tsx`
- 颜色、排版、黄色视觉系统、滚动转场和手机适配：`app/globals.css`
- 默认 SEO 与 HTML 语言：`app/layout.tsx`

后续可以继续在现有结构上修改，无需重新创建项目。
